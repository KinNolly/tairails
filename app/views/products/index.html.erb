<%= link_to "Login", new_session_path unless authenticated? %>
<h1>Sản Phẩm</h1>
<h1> Loai Banh </h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Action</th>
    </tr>
  </thead>
  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= product.name %></td>
        <td><%= link_to 'Xem Chi Tiết', product %></td>
      </tr>
    <% end %>
    <td><%= link_to "Thêm Sản Phẩm", new_product_path if authenticated?%></td>
  </tbody>
</table>